# Balanced Round

Choose the largest group of participants such that, after sorting their skill values, every adjacent pair in the chosen group differs by at most `k`.

Print the minimum number of participants to remove.

## Input

The first line contains integers `n` and `k`.
The second line contains `n` integers `a_i`.

## Output

Print one integer: the minimum number of removals.
