# Circle Distance

Given a circle radius and two angles in degrees, compute the shortest arc distance between the two points on the circle.

## Input

The input contains three numbers: `r`, `a`, and `b`.

## Output

Print the shortest arc length.
